// G6 v5 uses functional styling instead of registerNode
// No custom registration needed — styling is done via node.style callback
export function registerCustomNodes() {
  // G6 v5 provides built-in node types (rect, circle, etc.)
  // We use 'rect' with dynamic styling based on data.status
}
